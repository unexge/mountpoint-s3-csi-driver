---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.4
  name: mountpointclaims.s3.csi.aws.com
spec:
  group: s3.csi.aws.com
  names:
    kind: MountpointClaim
    listKind: MountpointClaimList
    plural: mountpointclaims
    singular: mountpointclaim
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MountpointClaim is the Schema for the mountpointclaims API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MountpointClaimSpec defines the desired state of MountpointClaim
            properties:
              nodeID:
                type: string
              podId:
                type: string
              volumeID:
                type: string
            type: object
          status:
            description: MountpointClaimStatus defines the observed state of MountpointClaim
            properties:
              mountpointPodID:
                type: string
              status:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
